"""
知识库数据 — 各岗位面试知识点。

结构：KNOWLEDGE_DOCS = [
    {"id": "唯一标识", "title": "来源标题", "text": "知识点正文"},
]

每条内容要求：能独立回答一个面试问题（定义 + 要点 + 追问 + 易错点），
这样检索结果本身就够用，LLM 补全空间小，回答有据可依。

扩充方式：往列表里加条目，然后重新运行 build_knowledge_base。
"""

KNOWLEDGE_DOCS = [
    # ============================================================
    #  后端开发方向
    # ============================================================
    {
        "id": "backend_http_https",
        "title": "后端·网络基础：HTTP/HTTPS 与 TCP",
        "text": (
            "HTTP 与 HTTPS 的区别：HTTPS = HTTP + TLS/SSL 加密层，端口 443，"
            "而 HTTP 是明文传输、端口 80。HTTPS 的握手流程是：客户端发起请求 → "
            "服务器返回证书 → 客户端验证证书 → 双方协商对称加密密钥 → 之后用对称加密传输数据。"
            "关键理解：非对称加密只用来安全地交换对称密钥，真正传数据用对称加密，因为对称加密快。"
            "TCP 三次握手：SYN → SYN+ACK → ACK，目的是确认双方收发能力都正常。"
            "四次挥手：因为 TCP 是全双工的，双方都要各自关闭，所以多了一次。"
            "HTTP/1.1 与 HTTP/2 的区别：HTTP/2 引入多路复用（一个连接并发多个请求）、"
            "头部压缩（HPACK）、二进制分帧，解决了 HTTP/1.1 的队头阻塞问题。"
            "追问点：为什么握手是三次不是两次？——两次无法确认客户端的发送能力（第二次 ACK 丢了服务器无法知道客户端是否收到）。"
            "易错点：不要把 HTTPS 理解成「加密的 HTTP」，它是在 HTTP 和 TCP 之间加了 TLS 层。"
        ),
    },
    {
        "id": "backend_db_index",
        "title": "后端·数据库：索引、事务与 MVCC",
        "text": (
            "MySQL 索引为什么用 B+ 树而不是哈希或红黑树：1. 磁盘 IO 是瓶颈，"
            "B+ 树矮胖（树高 3-4 层就能存千万级数据），减少磁盘 IO 次数；"
            "2. B+ 树叶子节点用链表连接，天然支持范围查询（ORDER BY、BETWEEN）；"
            "3. 红黑树太高、哈希不支持范围查询。"
            "聚簇索引与回表：主键索引（聚簇索引）的叶子节点存整行数据；"
            "二级索引（非主键索引）的叶子节点存主键值，查到主键后还要回到聚簇索引查整行，这就是回表。"
            "覆盖索引：如果查询的列都在二级索引里，就不用回表。"
            "事务隔离级别：读未提交（脏读）、读已提交（不可重复读）、可重复读（幻读，MySQL 默认）、串行化。"
            "MVCC 原理：每条记录有版本链（undo log），事务通过 ReadView 判断哪些版本可见，"
            "实现不加锁的并发读，解决不可重复读和大部分幻读。"
            "索引失效场景：对列做函数运算（WHERE YEAR(date) = 2024）、前导通配符（LIKE '%x'）、"
            "类型隐式转换（字符串列用数字查）、OR 连接非索引列。"
            "易错点：索引不是越多越好，每次写操作都要维护索引，索引多会降低写性能。"
        ),
    },
    {
        "id": "backend_redis",
        "title": "后端·Redis：缓存三大经典问题",
        "text": (
            "缓存穿透：查询一个根本不存在的数据（如用户 ID=-1），缓存永远不命中，"
            "请求直接打到数据库。核心特征：查的数据本身就不存在，不是缓存过期。"
            "解决方案：1. 缓存空值（查不到也缓存 null，设短过期时间 3-5 分钟）；"
            "2. 布隆过滤器（在缓存前拦截，判断 Key 不存在直接返回，O(1) 时间、内存占用极低，"
            "但有误判率、不支持删除）；3. 参数校验（非法参数直接拒绝，成本最低但只拦明显非法请求）。"
            "最优实践：三层防护——参数校验 → 布隆过滤器 → 缓存空值兜底，配合监控告警。"
            "缓存击穿：一个热点 Key 过期瞬间大量请求打到数据库。"
            "解决方案：互斥锁（只让一个请求去查库，其他等待）、逻辑过期（值不过期，后台异步更新）。"
            "缓存雪崩：大量 Key 同一时间过期，或 Redis 宕机，请求全部打到数据库。"
            "解决方案：过期时间加随机值（打散过期时间）、多级缓存、Redis 高可用（主从/集群）。"
            "易错点：三个问题必须区分清楚——穿透是「查不存在」，击穿是「热点 Key 过期」，雪崩是「大量 Key 同时过期」。"
            "追问点：布隆过滤器支持删除吗？不支持（多个 Key 可能映射到同一 bit 位），要删除用计数布隆过滤器或定期重建。"
        ),
    },
    {
        "id": "backend_redis_persistence",
        "title": "后端·Redis：持久化与分布式锁",
        "text": (
            "Redis 持久化两种方式：RDB（快照）——定期把内存数据快照存到磁盘，"
            "恢复快但可能丢失最后一次快照之后的数据；AOF（追加日志）——把每次写操作追加到日志，"
            "数据更完整但文件更大、恢复慢。生产常用混合持久化（RDB + AOF）。"
            "Redis 过期删除策略：惰性删除（访问时检查是否过期）+ 定期删除（定期抽样检查），"
            "两者配合，不是到点立刻删。"
            "分布式锁的正确实现：用 SET key value NX EX 秒数 一条命令原子地实现「加锁 + 设过期」，"
            "避免加锁后进程崩溃导致锁永不过期。锁续期用看门狗机制（如 Redisson），"
            "业务没执行完就自动续期。释放锁要用 Lua 脚本校验「只能删自己的锁」，防止误删别人的锁。"
            "易错点：1. SETNX 和 EXPIRE 分开两条命令不是原子的，中间崩溃会死锁；"
            "2. 直接 DEL 释放锁可能误删别人的锁。"
            "追问点：Redis 单线程为什么快？——纯内存操作 + IO 多路复用 + 单线程避免上下文切换和锁竞争。"
        ),
    },
    {
        "id": "backend_os_process",
        "title": "后端·操作系统：进程、线程、协程与 GIL",
        "text": (
            "进程与线程的区别：进程是资源分配的最小单位（独立内存空间），"
            "线程是 CPU 调度的最小单位（共享进程内存）。进程间通信靠 IPC（管道、消息队列、共享内存），"
            "线程间通信直接读写共享内存但要加锁。"
            "协程：用户态调度的轻量级执行单元，一个线程可以跑多个协程，"
            "切换成本远低于线程，适合 IO 密集型任务。"
            "Python GIL（全局解释器锁）：同一时刻只有一个线程能执行 Python 字节码，"
            "所以 Python 多线程在 CPU 密集型任务上无法利用多核。"
            "应对：CPU 密集用多进程（multiprocessing），IO 密集用多线程或协程（asyncio）。"
            "IO 多路复用：select（有 fd 数量限制 1024）、poll（无限制但仍需遍历）、"
            "epoll（事件驱动，活跃连接多时性能最好，Linux 专用）。"
            "死锁四个必要条件：互斥、持有并等待、不可剥夺、循环等待。破坏任一条件即可避免死锁。"
            "易错点：GIL 只影响 CPU 密集的 Python 多线程，IO 密集时线程会释放 GIL，多线程仍有意义。"
        ),
    },
    {
        "id": "backend_system_design",
        "title": "后端·系统设计方法论",
        "text": (
            "系统设计面试的标准回答框架（五步）："
            "1. 明确需求：功能需求（做什么）、非功能需求（QPS、延迟、可用性、数据量）；"
            "2. 估算规模：算 QPS 和数据量，判断是否需要分库分表、缓存；"
            "3. 设计架构：画图——负载均衡（Nginx/LVS）→ 应用层（无状态可水平扩展）→ 缓存（Redis）→ 数据库（主从）；"
            "4. 深入瓶颈：读写分离、分库分表（垂直/水平）、消息队列异步化、CDN；"
            "5. 讨论 trade-off：CAP 理论（一致性 vs 可用性）、成本 vs 性能、复杂度 vs 可维护性。"
            "面试官看重的不是答案，而是你能否结构化地分析问题、主动暴露 trade-off。"
            "秒杀系统设计：前端限流 → 网关限流 → 预扣库存（Redis 原子操作）→ 消息队列削峰 → 异步下单。"
            "易错点：一上来就画架构图，跳过需求澄清和规模估算，是最大的减分项。"
        ),
    },
    {
        "id": "backend_python",
        "title": "后端·Python 高频考点",
        "text": (
            "装饰器原理：装饰器本质是闭包——外层函数接收被装饰函数，返回一个包装函数，"
            "在包装函数里可以在调用前后加逻辑。常见应用：日志、鉴权、计时、缓存。"
            "迭代器与生成器：迭代器实现 __iter__ 和 __next__；生成器是特殊的迭代器，"
            "用 yield 惰性求值，内存占用低，适合处理大数据流。"
            "深拷贝与浅拷贝：浅拷贝（copy.copy）只复制对象本身，嵌套对象仍是引用；"
            "深拷贝（copy.deepcopy）递归复制所有嵌套对象。"
            "垃圾回收：Python 用引用计数为主，标记清除（解决循环引用）+ 分代回收（年轻代回收频繁）。"
            "asyncio：事件循环 + 协程，async/await 语法，适合 IO 密集，一个线程跑成千上万并发任务。"
            "可变对象默认参数陷阱：def f(x, lst=[]) 的 lst 是函数定义时创建一次，"
            "多次调用共享同一个列表，应该用 lst=None 再在函数内初始化。"
            "易错点：把多线程当成 Python 并发首选——CPU 密集应该用多进程。"
        ),
    },

    # ============================================================
    #  AI Agent / 大模型应用方向
    # ============================================================
    {
        "id": "agent_react",
        "title": "Agent·ReAct 模式原理",
        "text": (
            "ReAct（Reasoning + Acting）是 LLM Agent 的核心循环："
            "思考（Thought：决定下一步做什么）→ 行动（Action：调用工具）→ 观察（Observation：工具返回结果）→ "
            "再思考，循环直到得出最终答案（Finish）。"
            "为什么需要 ReAct：纯推理（Chain-of-Thought）会编造事实（幻觉），"
            "纯行动（只调工具）缺乏推理能力，ReAct 结合两者——推理指导行动，行动结果反哺推理。"
            "工程上的关键问题：1. LLM 决策不稳定，可能选错工具或死循环，"
            "要用代码状态机约束 LLM 的选择范围（constrained generation 思想）；"
            "2. 防死循环：设置 max_steps、已完成动作不可重复执行；"
            "3. 工具结果要存进 memory，供后续推理引用。"
            "手写 ReAct 的意义：理解 AgentExecutor 内部就是 for 循环 + 工具调用，"
            "才能理解 LangGraph 的节点和 conditional_edge 是对同一思想的抽象。"
            "追问点：ReAct 和 function calling 的区别？——ReAct 是显式的思考-行动循环，"
            "function calling 是模型直接输出要调用的函数，思考过程隐式。"
        ),
    },
    {
        "id": "agent_langchain",
        "title": "Agent·LangChain 核心概念",
        "text": (
            "LangChain 四大核心概念："
            "1. Chain：把多个固定步骤串成流水线（如 解析文档 → 切分 → 向量化），流程固定；"
            "2. Agent：由 LLM 动态决定调用哪个工具、按什么顺序，比 Chain 灵活；"
            "3. Tool：用 @tool 装饰器把普通函数标记为可调用工具，给 LLM 描述用途和参数；"
            "4. Memory：会话记忆，策略有 ConversationBufferMemory（全量存）、"
            "ConversationSummaryMemory（摘要）、ConversationBufferWindowMemory（滑动窗口）。"
            "AgentExecutor 内部原理：就是一个循环——调 LLM 得到动作 → 执行工具 → 把结果追加到消息 → "
            "再调 LLM，直到返回 Finish。理解了这个，任何 Agent 框架都是语法糖。"
            "理解底层 vs 调包：面试能讲清楚「AgentExecutor 的 for 循环」和「代码状态机 = LangGraph conditional_edge」，"
            "才是区分点。否则就是「我用过 LangChain 但不知道它怎么工作」。"
            "易错点：把 LangChain 当成必须用的框架——它只是规范化的模板代码，手写一遍更理解本质。"
        ),
    },
    {
        "id": "agent_rag",
        "title": "Agent·RAG 检索增强生成",
        "text": (
            "RAG（Retrieval-Augmented Generation）解决两个问题：LLM 幻觉（编造事实）"
            "和知识过时（训练数据有截止日期）。做法是把外部知识检索后喂给 LLM，让它基于检索结果生成。"
            "完整流程：1. 文档切片（chunking）；2. 切片 embedding 向量化；3. 向量存入向量库；"
            "4. 用户查询向量化；5. 相似度检索 top-k；6. 检索结果 + 问题一起喂给 LLM 生成答案。"
            "Hybrid Search（混合检索）：向量检索擅长语义匹配（「后端开发」能匹配「服务端工程师」），"
            "但对精确关键词不敏感（搜「Python 3.12」可能返回 Python 3.11）；"
            "BM25 擅长精确关键词匹配。两者互补，用 RRF（Reciprocal Rank Fusion）融合——"
            "基于排名而非分数（两个检索器分数尺度不同，排名可比），公式 score = Σ 1/(k + rank)，k 取 60。"
            "Reranker（重排）：向量检索和 BM25 是双塔模型（query 和 doc 分别编码，快但精度低），"
            "Reranker 是交叉编码（query 和 doc 一起输入，注意力机制跨交互，慢但精度高）。"
            "实际架构：粗排用向量+BM25 快速筛 top-20，精排用 cross-encoder 重排 top-5。"
            "来源标注：RAG 回答必须可溯源（哪句话来自哪篇文档），这是 RAG 区别于纯 LLM 的关键卖点。"
        ),
    },
    {
        "id": "agent_llm_engineering",
        "title": "Agent·LLM 应用工程化",
        "text": (
            "Prompt Engineering 核心技巧：1. 角色设定（你是一位资深 HR）；2. 结构化输出（指定 JSON/Markdown 格式）；"
            "3. few-shot 示例（给几个输入输出范例）；4. 思维链（让模型一步步推理）。"
            "Token 成本控制：1. 上下文窗口管理——system prompt 优先、业务记忆次之、对话历史最后；"
            "2. 摘要压缩——早期对话用 LLM 压缩成摘要替代直接丢弃；3. 近期优先截断——最近消息保留原文。"
            "微调 vs RAG 的选择：RAG 适合知识动态更新、成本低、可溯源；"
            "微调适合改变模型行为风格（语气、格式）、需要数据集和算力、成本高。大多数场景先 RAG。"
            "流式输出：SSE 逐 token 推送提升体验，但流式不做自动重试（文字已渲染，重试会闪断）。"
            "LLM 输出不可信：必须代码兜底——校验 JSON 格式、设置超时重试、失败降级（如摘要失败退化为截断）。"
            "工程原则：规则由代码保证（状态机、格式校验），LLM 只负责语义判断和内容生成。"
        ),
    },
    {
        "id": "agent_sse_streaming",
        "title": "Agent·流式输出：SSE 与 WebSocket",
        "text": (
            "SSE（Server-Sent Events）vs WebSocket："
            "SSE 是单向推送（服务器→客户端），基于 HTTP，自动重连，浏览器 EventSource 原生支持；"
            "WebSocket 是全双工（双向），ws:// 协议，需要额外库。"
            "AI 流式回答为什么选 SSE：只需要服务器推给客户端，单向就够，SSE 更简单、代理兼容性更好。"
            "FastAPI 实现：StreamingResponse + media_type='text/event-stream'，"
            "事件格式是 event: xxx\\ndata: {json}\\n\\n。"
            "Nginx 关键配置：proxy_buffering off，否则流式输出会被缓冲成一大块，用户等半天才看到。"
            "EventSource 的局限：只支持 GET，需要 POST（带请求体）时要用 fetch + ReadableStream 手动解析 SSE 流。"
            "流式不重试的原因：用户已经看到前半段文字，重试会导致文字消失重来，体验比报错更差。"
            "易错点：SSE 事件之间用空行分隔（\\n\\n），解析时要注意 buffer 中可能只有半个事件。"
        ),
    },

    # ============================================================
    #  前端方向
    # ============================================================
    {
        "id": "frontend_js_core",
        "title": "前端·JS 核心：闭包、原型与事件循环",
        "text": (
            "闭包：函数能记住并访问其定义时的词法作用域，即使函数在别处执行。"
            "应用：防抖节流、私有变量、函数柯里化。"
            "原型链：每个对象有 __proto__ 指向构造函数的 prototype，"
            "访问属性时沿原型链向上查找，这是 JS 的继承机制。"
            "事件循环：JS 是单线程，同步代码先执行，异步任务进任务队列——"
            "宏任务（setTimeout、setInterval、I/O）和微任务（Promise.then、async/await）。"
            "每轮循环：先执行同步代码 → 清空微任务队列 → 取一个宏任务执行 → 再清空微任务。"
            "this 指向：普通函数看调用方式（谁调用指向谁），箭头函数看定义时的外层作用域。"
            "防抖 vs 节流：防抖是延迟执行（连续触发只执行最后一次，适合搜索输入）；"
            "节流是固定频率执行（适合滚动、拖拽）。"
            "易错点：Promise.then 是微任务，setTimeout 是宏任务，微任务优先级更高，执行顺序容易搞混。"
        ),
    },
    {
        "id": "frontend_vue",
        "title": "前端·Vue 响应式与组件",
        "text": (
            "Vue 响应式原理：Vue3 用 Proxy 拦截对象的 get/set，依赖收集 + 派发更新；"
            "Vue2 用 Object.defineProperty（无法监听新增属性、数组下标变化，需要 $set）。"
            "Composition API（组合式 API）：ref（基础类型响应式）、reactive（对象响应式）、"
            "computed（缓存的计算属性）、watch（监听变化）。"
            "组件通信：父传子 props、子传父 emit、跨层级 provide/inject、全局状态管理 Pinia。"
            "单向数据流：状态集中在 composable/store，子组件纯展示（props 接收 + emit 通知），"
            "子组件不直接改父组件状态——这是 Vue 官方推荐模式。"
            "虚拟 DOM 与 diff：数据变化 → 生成新虚拟 DOM → 与旧树 diff（同层比较、key 复用）→ 最小化 DOM 操作。"
            "易错点：ref 在模板中自动解包，但在 script 里要 .value；defineModel 需要 Vue 3.4+。"
        ),
    },
    {
        "id": "frontend_http_browser",
        "title": "前端·浏览器与跨域",
        "text": (
            "浏览器渲染流程：解析 HTML 生成 DOM 树 → 计算 CSS 生成 CSSOM → 合成渲染树 → "
            "布局（layout，计算位置）→ 绘制（paint）→ 合成（composite）。"
            "跨域（CORS）：浏览器同源策略（协议+域名+端口都相同才算同源）阻止跨域请求。"
            "CORS 机制：浏览器发跨域请求时先发 OPTIONS 预检请求，服务器返回 Access-Control-Allow-* 头，"
            "浏览器根据响应决定是否放行。简单请求（GET/POST、无自定义头）不发预检。"
            "跨域解决方案：CORS（标准）、JSONP（仅 GET、老方案）、代理（开发环境 Vite proxy，最省事）。"
            "本地开发跨域坑：localhost 和 127.0.0.1 被浏览器视为不同源，跨域策略不一致，"
            "用代理从根源消灭跨域比反复调 CORS 更可靠。"
            "存储对比：localStorage（5MB、永久）、sessionStorage（5MB、会话级）、Cookie（4KB、可随请求发送、可设 httpOnly）。"
            "浏览器缓存：强缓存（Cache-Control: max-age，直接不请求）和协商缓存（ETag/Last-Modified，304 响应）。"
        ),
    },
    {
        "id": "frontend_perf",
        "title": "前端·性能优化",
        "text": (
            "前端性能优化四大方向："
            "1. 加载优化：代码分割（按路由懒加载）、CDN 加速、压缩（gzip/brotli）、资源预加载；"
            "2. 渲染优化：减少重排重绘（批量改 DOM、用 transform 代替 top/left）、虚拟列表（长列表只渲染可见区）；"
            "3. 网络优化：HTTP/2 多路复用、合理设置缓存策略、图片懒加载 + WebP 格式；"
            "4. 体积优化：tree-shaking（摇树删除无用代码）、按需引入（如 lodash 按函数引入）。"
            "首屏优化：SSR（服务端渲染直出 HTML）、骨架屏、关键渲染路径优化（内联首屏 CSS、延迟非关键 JS）。"
            "易错点：性能优化要先测量（Performance、Lighthouse）再优化，"
            "不要凭感觉优化——「过早优化是万恶之源」。"
        ),
    },

    # ============================================================
    #  算法方向
    # ============================================================
    {
        "id": "algo_common",
        "title": "算法·高频题型与解法",
        "text": (
            "LRU 缓存：哈希表（O(1) 查找）+ 双向链表（O(1) 移动），"
            "访问时移到链表头，满了删链表尾。Python 可用 OrderedDict 或 collections 实现。"
            "反转链表：迭代法（三个指针 prev/curr/next 逐个反转）和递归法（先反转子链再连当前节点）。"
            "TopK 问题：用小顶堆（大小为 K，O(nlogK)）或快速选择（平均 O(n)）。"
            "两数之和：哈希表存值到索引，O(n)；三数之和：排序 + 双指针，O(n^2)。"
            "二叉树遍历：前序（根左右）、中序（左根右）、后序（左右根）、层序（BFS 队列），递归和迭代都要会。"
            "字符串：最长无重复子串（滑动窗口）、最长公共前缀（纵向比较）。"
            "易错点：边界条件——空链表、单节点、K 大于数组长度，面试时主动问清再写。"
        ),
    },
    {
        "id": "algo_complexity",
        "title": "算法·复杂度分析",
        "text": (
            "时间复杂度：大 O 表示法，关注问题规模增大时执行次数的增长趋势，忽略常数和低阶项。"
            "空间复杂度：算法运行额外占用的空间，包括递归的调用栈。"
            "常见复杂度排序：O(1) < O(logn) < O(n) < O(nlogn) < O(n^2) < O(2^n) < O(n!)。"
            "典型例子：二分查找 O(logn)、快排/归并 O(nlogn)、冒泡/插入 O(n^2)、"
            "斐波那契递归 O(2^n)、全排列 O(n!)。"
            "均摊分析：如动态数组扩容，单次操作可能 O(n)，但均摊到每次是 O(1)。"
            "面试要求：能说出最优解、次优解的复杂度，以及为什么最优——"
            "「为什么快排平均 O(nlogn) 但最坏 O(n^2)」（pivot 选得不好导致每层只切一个）。"
        ),
    },
    {
        "id": "algo_advanced",
        "title": "算法·动态规划与图",
        "text": (
            "动态规划适用条件：最优子结构（大问题的最优解包含子问题的最优解）+ 重叠子问题（子问题被重复计算）。"
            "解题步骤：定义 dp 数组含义 → 找状态转移方程 → 初始化 → 确定遍历顺序。"
            "经典题：最长递增子序列（dp[i] 表示以 i 结尾的最长）、背包问题（01 背包和完全背包）、"
            "编辑距离（两字符串转换最少操作数）、爬楼梯（斐波那契）。"
            "图论：DFS（栈/递归，适合找路径、连通性）、BFS（队列，适合最短路径无权图）、"
            "Dijkstra（加权图最短路径，贪心）、拓扑排序（有向无环图，依赖关系）。"
            "贪心：每步选局部最优，若局部最优能推出全局最优则可用（如找零钱、活动安排）。"
            "双指针与滑动窗口：子数组/子串问题（和为定值、最长无重复）。"
            "易错点：DP 状态定义不清就写转移方程，是初学者最大问题——先把 dp[i] 的含义说清楚。"
        ),
    },

    # ============================================================
    #  产品经理方向
    # ============================================================
    {
        "id": "pm_product_sense",
        "title": "产品·产品思维与优先级",
        "text": (
            "产品经理面试核心考察：需求分析、优先级判断、数据分析、竞品分析、用户增长。"
            "需求分析：用户是谁（画像）、痛点是什么（真实且高频）、解决什么价值（可量化）。"
            "优先级判断工具：价值/成本矩阵（高价值低成本先做）、RICE 模型（Reach 触达 × Impact 影响 × Confidence 信心 ÷ Effort 成本）。"
            "数据分析：北极星指标（最能代表产品价值的核心指标）、漏斗分析（每个转化环节的流失）。"
            "竞品分析：差异化定位——不追求全面超越，而是找到「人无我有、人有我优」的点。"
            "用户增长：AARRR 模型（获取 Acquisition、激活 Activation、留存 Retention、收入 Revenue、传播 Referral）。"
            "易错点：把功能堆砌当产品能力——产品经理的核心是判断「做什么、不做什么」，不是「能做多少」。"
        ),
    },
    {
        "id": "pm_case",
        "title": "产品·案例分析框架",
        "text": (
            "产品案例分析题标准框架（五步）："
            "1. 明确问题：界定范围、目标用户、核心场景（不要急着给答案）；"
            "2. 拆解问题：按维度拆解（用户维度、场景维度、功能维度）；"
            "3. 提出方案：至少两个方案并对比优劣（一个保守、一个激进）；"
            "4. 评估方案：用数据或逻辑论证（谁的用户量最大、成本最低、可落地）；"
            "5. 总结：给出推荐方案和理由，说明风险和验证方式。"
            "面试官看重的：结构化思维（不是唯一正确答案）、用户视角（不是功能视角）、"
            "trade-off 意识（任何方案都有代价）。"
            "常见题型：「设计一个 XX 功能」、「如果 XX 数据下降了怎么办」、「如何提升 XX 指标」。"
        ),
    },

    # ============================================================
    #  测试/QA 方向
    # ============================================================
    {
        "id": "qa_test_design",
        "title": "测试·用例设计方法",
        "text": (
            "测试用例设计方法："
            "1. 等价类划分：把输入域分成若干等价类（有效/无效），每个类取一个代表值；"
            "2. 边界值分析：重点测边界值（0、最大值、空值、越界值），bug 常藏在边界；"
            "3. 因果图/判定表：复杂逻辑（多条件组合）用判定表穷举；"
            "4. 场景法：模拟用户真实操作路径（正常流程 + 异常流程）；"
            "5. 错误推测法：根据经验推测易错点（如并发、空数据、特殊字符）。"
            "好用例的标准：可执行、可重复、有明确预期、最小化（一条用例测一个点）。"
            "易错点：只测正常流程，不测边界和异常——面试时主动说明异常用例的覆盖是加分项。"
        ),
    },
    {
        "id": "qa_automation",
        "title": "测试·自动化与接口测试",
        "text": (
            "接口测试要点：请求方法（GET/POST/PUT/DELETE）、参数（必填/选填/边界）、"
            "状态码（200/400/401/404/500）、响应体断言（字段、类型、值）。"
            "自动化测试框架：Python 用 pytest + requests（或 httpx），Java 用 JUnit + RestAssured。"
            "单元测试的 mock：把外部依赖（如 LLM API、数据库）替换成假实现，"
            "专注测自身逻辑——mock 隔离是单元测试的核心。"
            "测试金字塔：单元测试最多（快、稳定）、集成测试次之、端到端最少（慢、脆弱）。"
            "CI 集成：代码提交自动跑测试，失败阻断合并。"
            "易错点：测试用例本身有 bug（测错了东西）、过度依赖端到端测试（又慢又不稳定）。"
        ),
    },

    # ============================================================
    #  求职通用
    # ============================================================
    {
        "id": "job_resume",
        "title": "求职·简历撰写技巧",
        "text": (
            "简历撰写核心技巧："
            "1. STAR 法则描述项目：情境（Situation）、任务（Task）、行动（Action）、结果（Result）；"
            "2. 量化成果：性能提升 30%、服务 100 万用户、节省 2 人天，数字比形容词有说服力；"
            "3. 技术栈写具体，避免写「精通」（除非真的精通，否则面试会被深挖）；"
            "4. 针对目标岗位定制关键词，匹配 JD 里的要求（很多公司用 ATS 筛选关键词）；"
            "5. 一页纸原则，突出最近 2-3 年最相关的经历；"
            "6. 项目描述用三段式：为什么这样做 → 有什么优缺点 → 如果重来怎么改进，"
            "这正好是面试官会追问的方向，简历里埋好伏笔。"
            "易错点：堆砌技术名词但没有项目支撑、把课程作业当项目写、经历和岗位不相关。"
        ),
    },
    {
        "id": "job_behavioral",
        "title": "求职·行为面试",
        "text": (
            "行为面试（STAR 法则）常见问题："
            "1. 讲一个最有挑战的项目——重点突出你负责的部分、遇到的困难、解决的思路（不是项目本身多牛）；"
            "2. 与同事分歧怎么处理——对事不对人、用数据说话、寻求共识、必要时上升决策；"
            "3. 如何学习新技术——项目驱动、官方文档、动手实践、输出总结；"
            "4. 讲一次失败经历——重点在反思和成长（「我学到了什么、下次怎么做」）；"
            "5. 职业规划——短期深耕技术、长期成为领域专家，要具体（不要说「成为优秀的人」）。"
            "独立项目如何补团队协作短板：主动说明「如果有团队协作场景，我的分工边界是什么」，"
            "以及你如何做技术文档、代码规范、review——体现协作意识。"
            "易错点：把「我们团队做了什么」说成「我做了什么」（面试官会追问细节），或者只说结果不说过程。"
        ),
    },

    # ============================================================
    #  【区分度扩展】以下为高频主题的「词面重叠」补充文档
    #  目的：让同一主题下有多篇词面接近的文档，检索必须精确区分，
    #        否则纯向量/纯 BM25 会被近义词面带偏——这是评测有区分度的前提。
    # ============================================================

    # ---------- Redis 主题（4 篇词面重叠：穿透/击穿/雪崩 之外，再加锁/集群/淘汰） ----------
    {
        "id": "backend_redis_distributed_lock",
        "title": "后端·Redis：分布式锁的加锁、续期与释放",
        "text": (
            "Redis 分布式锁的完整实现：加锁用 SET key value NX EX 秒数 一条命令原子完成，"
            "value 用唯一标识（如 UUID + 线程号）标识持锁者；续期用看门狗（watchdog）定时延长过期时间，"
            "防止业务没执行完锁就过期；释放锁用 Lua 脚本校验「只能删自己的锁」，避免误删。"
            "为什么 SETNX + EXPIRE 分开两条命令会出错：两个命令之间进程崩溃，锁没有过期时间就变成死锁。"
            "锁过期时间设置多久：要大于业务最长执行时间，否则业务还没执行完锁就被别的线程拿到。"
            "追问点：Redisson 的看门狗默认多久续一次期？——锁过期时间的 1/3，比如 30 秒锁每 10 秒续一次。"
            "易错点：直接 DEL 释放锁会误删别人的锁；锁的粒度要小，粗粒度锁会严重降低并发。"
        ),
    },
    {
        "id": "backend_redis_cluster",
        "title": "后端·Redis：集群、哨兵与主从复制",
        "text": (
            "Redis 高可用的三种方案：主从复制（master 写、slave 读，读写分离，但主挂了不能自动切换）；"
            "哨兵模式（Sentinel 监控主从，主挂了自动选举新主，需要哨兵集群避免单点）；"
            "集群模式（Redis Cluster，数据分片到 16384 个 slot，节点间 gossip 通信，支持水平扩展）。"
            "主从复制的延迟问题：从库读可能读到旧数据，一致性要求高的场景要读主库。"
            "脑裂问题：哨兵和主库网络隔离时，哨兵可能选举出新主，出现两个主写数据——"
            "用 min-slaves-to-write 配置限制「从库太少时主库拒绝写入」。"
            "追问点：Redis Cluster 为什么是 16384 个 slot？——节点间心跳用位图传递 slot 信息，16384 能让位图紧凑。"
            "易错点：把主从复制当成高可用（它解决读写分离和备份，不解决自动故障转移）。"
        ),
    },
    {
        "id": "backend_redis_eviction",
        "title": "后端·Redis：内存淘汰与过期删除策略",
        "text": (
            "Redis 内存满了怎么办：用 maxmemory 限制最大内存，配合内存淘汰策略（eviction policy）"
            "决定淘汰哪些 key。常见策略：noeviction（不淘汰、写入报错）、allkeys-lru（所有 key 里淘汰最久未用）、"
            "volatile-lru（只淘汰设了过期时间的 key）、allkeys-lfu（淘汰访问频率最低）。"
            "过期删除策略（和淘汰是两回事）：惰性删除（访问时检查是否过期）+ 定期删除（定期抽样检查），"
            "两者配合，不是到点立刻删。过期但没删的 key 仍在内存里，叫惰性过期。"
            "缓存淘汰 vs 过期删除的区别：淘汰是「内存满了主动腾地方」，过期是「key 到时间了该删」，触发条件不同。"
            "追问点：为什么默认不是 lru 而是 noeviction？——生产怕误删重要数据，通常显式配置。"
            "易错点：把过期删除当成淘汰策略；expire 到期不等于立即释放内存。"
        ),
    },
    {
        "id": "backend_redis_transaction",
        "title": "后端·Redis：事务、管道与 Lua 脚本",
        "text": (
            "Redis 事务用 MULTI / EXEC / DISCARD / WATCH 实现，特点：命令入队后一起执行，"
            "但「不支持回滚」——某条命令执行失败，其他命令照样执行，这是 Redis 事务和数据库事务的本质区别。"
            "WATCH 实现乐观锁：监控某个 key，EXEC 时如果 key 被改过，事务不执行。"
            "管道（pipeline）：把多条命令打包一次发送，减少网络往返（RTT），不是原子操作。"
            "Lua 脚本：脚本在 Redis 服务端原子执行（Redis 单线程），适合「读-判断-写」这类需要原子性的操作，"
            "比如分布式锁的释放。脚本执行期间其他命令要排队，所以脚本不能太长。"
            "追问点：事务和 Lua 脚本的原子性有什么不同？——事务能保证一组命令连续执行，但命令之间可能穿插别的客户端命令；"
            "Lua 脚本真正独占执行，原子性更强。"
            "易错点：以为 Redis 事务像数据库事务一样能回滚——它不能。"
        ),
    },

    # ---------- MySQL 主题（4 篇词面重叠：索引之外，加锁/日志/隔离） ----------
    {
        "id": "backend_mysql_lock",
        "title": "后端·MySQL：行锁、表锁与死锁",
        "text": (
            "MySQL 锁的层次：全局锁（备份时用）、表锁（MyISAM 默认，粒度大）、行锁（InnoDB 默认，粒度小、并发高）。"
            "行锁在索引上的体现：InnoDB 行锁是加在索引记录上的，不是加在数据行——"
            "如果 SQL 没走索引，行锁会退化成表锁，这是「索引失效导致锁升级」的经典问题。"
            "死锁的产生：两个事务互相持有对方需要的锁（A 锁了行1 等行2，B 锁了行2 等行1）。"
            "死锁的解决：MySQL 有死锁检测，检测到就回滚一个事务；应用层可以按固定顺序加锁避免死锁。"
            "间隙锁（gap lock）与临键锁（next-key lock）：可重复读隔离级别下，为防幻读，"
            "InnoDB 会对索引间隙加锁，这也是可重复读下幻读被解决的原因。"
            "追问点：为什么「没走索引」会锁全表？——因为行锁依赖索引定位，没索引只能锁所有行。"
            "易错点：以为行锁就是锁住那一行数据，实际锁的是索引记录。"
        ),
    },
    {
        "id": "backend_mysql_binlog_redo",
        "title": "后端·MySQL：binlog、redo log 与两阶段提交",
        "text": (
            "redo log（重做日志）：InnoDB 引擎层的日志，记录「数据页做了什么修改」，"
            "用于崩溃恢复（掉电后重放 redo log 恢复已提交但没落盘的数据）。"
            "binlog（归档日志）：Server 层的逻辑日志，记录「执行了什么 SQL」，"
            "用于主从复制、数据恢复、审计。两者是不同层的日志，不能混为一谈。"
            "两阶段提交（2PC）：事务提交时，先写 redo log（prepare 阶段）→ 写 binlog → redo log 标记 commit，"
            "保证 redo log 和 binlog 一致——否则主从复制后从库数据和主库不一致。"
            "追问点：为什么要有两个日志？——redo log 是引擎层物理日志（快、崩溃恢复），"
            "binlog 是 Server 层逻辑日志（复制、恢复），职责不同。"
            "易错点：把 binlog 和 redo log 当成同一个东西，它们层次、格式、用途都不同。"
        ),
    },
    {
        "id": "backend_mysql_isolation",
        "title": "后端·MySQL：事务隔离级别与脏读幻读",
        "text": (
            "事务四大特性 ACID：原子性（undo log）、一致性、隔离性（锁 + MVCC）、持久性（redo log）。"
            "四个隔离级别及解决的问题：读未提交（脏读）、读已提交（脏读解决，不可重复读）、"
            "可重复读（脏读、不可重复读解决，MySQL 默认，幻读大部分解决）、串行化（全部解决，性能最差）。"
            "脏读 / 不可重复读 / 幻读的区别：脏读是「读到别人未提交的数据」；"
            "不可重复读是「同一事务两次读同一行，值变了」（别的事务改了并提交）；"
            "幻读是「同一事务两次查询，行数变了」（别的事务插入了新行）。"
            "可重复读如何解决幻读：MVCC（多版本并发控制）的快照读 + 间隙锁（当前读），两者配合。"
            "追问点：可重复读真的彻底解决幻读了吗？——快照读解决了，当前读靠间隙锁解决，严格说不是完全免疫。"
            "易错点：把不可重复读和幻读搞混——前者是「值变了」，后者是「行数变了」。"
        ),
    },

    # ---------- Agent 主题（3 篇词面重叠：LangChain 之外，加记忆/规划） ----------
    {
        "id": "agent_memory",
        "title": "Agent·记忆机制：短期、长期与向量记忆",
        "text": (
            "Agent 的记忆分层（按时间尺度）：短期记忆（会话内，Redis/内存，TTL 过期）；"
            "长期记忆（跨会话，SQLite/数据库，持久化）；工作记忆（单次任务内的中间状态）。"
            "对话记忆的几种策略：ConversationBufferMemory（全量存，长对话会爆上下文）、"
            "ConversationBufferWindowMemory（滑动窗口，只保留最近 N 条）、"
            "ConversationSummaryMemory（LLM 摘要，省 token 但有信息损失）、混合（早期摘要 + 近期原文）。"
            "向量记忆（MemGPT 式）：把记忆 embedding 后语义检索相关片段，配重要性评分和遗忘衰减——"
            "适合记忆量大、需要按相关性召回的复杂 Agent，对简单场景是过度设计。"
            "追问点：为什么不能只有一层记忆？——不同时间尺度的需求不同，短期要快、长期要持久、"
            "长对话要压缩，单层方案无法同时满足。"
            "易错点：把所有记忆都塞进 prompt，导致上下文爆炸；或不区分业务记忆和对话记忆。"
        ),
    },
    {
        "id": "agent_planning",
        "title": "Agent·任务规划：ReAct 之外的规划范式",
        "text": (
            "Agent 的任务规划范式，从简单到复杂："
            "1. ReAct：思考-行动-观察循环，每步由 LLM 决定下一步做什么，适合中等复杂度任务；"
            "2. Plan-and-Execute（规划-执行分离）：先让 Planner 一次性生成完整计划，再让 Executor 逐步执行，"
            "适合步骤明确的任务，计划错了可以重新规划；"
            "3. 树搜索（Tree of Thoughts / A*）：维护多个候选方案树，回溯搜索最优解，"
            "适合需要探索多路径的推理任务，但 token 消耗巨大。"
            "规划器（Planner）和执行器（Executor）分离的好处：职责单一，规划可复用、可重试。"
            "追问点：什么时候 ReAct 不够用？——任务步骤多、依赖复杂、需要全局规划时，ReAct 的逐步决策容易短视。"
            "易错点：所有任务都用树搜索（成本爆炸）；或把「规划」理解成只是「分步骤」，忽略了回溯和重规划。"
        ),
    },

    # ---------- 并发主题（2 篇词面重叠：线程安全之外，加异步） ----------
    {
        "id": "backend_concurrency_safe",
        "title": "后端·并发：线程安全、锁与原子操作",
        "text": (
            "线程安全问题：多个线程同时读写共享变量，导致结果不确定（竞态条件）。"
            "解决手段：加锁（互斥锁 mutex，保证同一时刻只有一个线程访问）；原子操作（如 Python 的 threading.Lock、"
            "atomic 变量、Redis 的 INCR）；无锁数据结构（CAS 乐观锁）。"
            "互斥锁 vs 乐观锁：互斥锁悲观（先锁再改，阻塞）；乐观锁乐观（先改，提交时检查版本/时间戳有没有被改）。"
            "死锁的四个必要条件：互斥、持有并等待、不可剥夺、循环等待——破坏任一即可避免。"
            "追问点：Python 里为什么有 GIL 还要加锁？——GIL 保证字节码层面串行，但「读-改-写」这种复合操作"
            "中间仍可能被切换，非原子操作还是要加锁。"
            "易错点：以为有 GIL 或「单线程语言」就不用考虑并发安全。"
        ),
    },
    {
        "id": "backend_async_vs_thread",
        "title": "后端·并发：多线程、多进程与异步 IO 怎么选",
        "text": (
            "三种并发模型的适用场景：多线程（IO 密集，共享内存，切换开销小，但有 GIL/锁问题）；"
            "多进程（CPU 密集，隔离好，但进程间通信成本高、占内存）；"
            "异步 IO（asyncio，高并发 IO，单线程事件循环，协程切换，适合大量网络请求）。"
            "Python 的选择：CPU 密集用多进程（GIL 限制多线程），IO 密集用多线程或协程。"
            "异步 IO 的原理：事件循环 + 协程，IO 等待时让出执行权，一个线程跑成千上万并发任务，"
            "避免线程切换开销和锁竞争。"
            "追问点：异步 IO 能提升 CPU 密集任务的性能吗？——不能，它解决的是 IO 等待，不是计算。"
            "易错点：CPU 密集任务用多线程（受 GIL 限制），或 IO 密集任务用多进程（进程开销大）。"
        ),
    },

    # ============================================================
    #  【第一批扩量】LLM/Agent 深入 + 后端基础 + 算法 + 安全
    #  目标：知识库 36 → 60 篇，为 80 条评测集提供足够的文档基础
    # ============================================================

    # ---------- LLM 基础（4 篇：注意力/微调/推理/提示） ----------
    {
        "id": "llm_attention",
        "title": "大模型·注意力机制与 Transformer",
        "text": (
            "Transformer 的核心是自注意力（self-attention）：对序列中的每个 token，"
            "计算它与其他所有 token 的相关性权重，加权求和得到新的表示——"
            "这样每个 token 都能「看到」整个序列的上下文。"
            "Q/K/V 三个矩阵的作用：Query（我要找什么）、Key（我有什么标签）、Value（实际内容），"
            "注意力分数 = softmax(QK^T / √d_k)，再乘以 V。除以 √d_k 是为了防止点积过大导致 softmax 梯度消失。"
            "多头注意力：把 Q/K/V 拆成多个头，各自计算再拼接，让模型从不同子空间捕捉关系。"
            "位置编码：Transformer 本身没有顺序概念，需要位置编码（正弦位置编码或可学习位置编码）注入位置信息。"
            "追问点：自注意力为什么比 RNN 好？——可以并行计算（不像 RNN 串行），且能直接建模任意距离的依赖。"
            "易错点：把 Q/K/V 理解成「查询数据库」的比喻而忘了它们是可学习的线性变换。"
        ),
    },
    {
        "id": "llm_finetune",
        "title": "大模型·微调：LoRA、PEFT 与全参微调",
        "text": (
            "微调的分类：全参微调（更新所有参数，效果好但需要大算力）；"
            "参数高效微调 PEFT（只更新少量参数，LoRA/Adapter/Prefix-tuning 都属于这类）。"
            "LoRA 的原理：冻结原模型权重，在旁路加两个低秩矩阵 A、B（秩 r 很小），"
            "只训练 A、B，更新量 ΔW = BA。好处：可训练参数从千亿降到百万级，单卡就能微调大模型，"
            "且可以存多个 LoRA 权重随时切换任务。"
            "LoRA 的秩 r 怎么选：r 太小表达能力不足，r 太大接近全参，常见 8~64。"
            "微调 vs RAG：RAG 适合知识动态更新、成本低、可溯源；微调适合改变模型行为风格（语气、格式），需要数据集和算力。"
            "追问点：为什么 LoRA 能起作用？——大模型微调时权重变化是低秩的，全参微调的增量矩阵可以用低秩近似。"
            "易错点：以为 LoRA 不改变模型知识（它主要改变风格和格式，不适合灌入大量新知识）。"
        ),
    },
    {
        "id": "llm_inference_opt",
        "title": "大模型·推理优化：KV Cache、量化与投机采样",
        "text": (
            "KV Cache：生成时每一步都要算注意力，而之前 token 的 K、V 已经算过了，"
            "缓存它们避免重复计算，把生成从 O(n^2) 降到 O(n)。代价是显存随序列长度线性增长。"
            "量化：把 FP32/FP16 权重降到 INT8/INT4，减少显存和计算量，"
            "常见方案 GPTQ（训练后量化）、AWQ（激活感知权重）、bitsandbytes。代价是精度略有损失。"
            "投机采样（speculative decoding）：用小模型快速草稿生成多个 token，再用大模型一次性验证，"
            "验证通过的接受，不通过的重新生成——用小模型换大模型的吞吐。"
            "上下文长度扩展：RoPE 旋转位置编码 + 插值，把 4k 上下文扩到 32k 甚至更长。"
            "追问点：量化为什么能减少显存？——权重用更少 bit 存储，INT8 是 FP32 的四分之一。"
            "易错点：以为 KV Cache 是「缓存整个注意力矩阵」（它缓存的是 K、V 向量，不是注意力分数）。"
        ),
    },
    {
        "id": "llm_prompting",
        "title": "大模型·提示工程：CoT、Few-shot 与结构化输出",
        "text": (
            "思维链（Chain-of-Thought, CoT）：让模型「一步步推理」而不是直接给答案，"
            "显著提升数学、逻辑等需要多步推理的任务表现。"
            "Few-shot：给几个输入输出示例，让模型学会任务格式和期望，比零样本更稳定。"
            "Zero-shot CoT：不举例，只加一句「让我们一步步思考」也能激发推理。"
            "结构化输出：要求模型输出 JSON/Markdown 等格式，配合 JSON mode 或 function calling 保证格式合法。"
            "但注意：JSON mode 只保证「格式合法」，不保证「内容语义正确」——格式和语义是两个问题。"
            "追问点：CoT 为什么有效？——把隐式推理显式化，给模型更多「思考空间」，减少一步到位出错。"
            "易错点：把 prompt 当万能钥匙（能解决的问题有限）；或以为 JSON mode 能解决语义越界（它只解决格式）。"
        ),
    },

    # ---------- Agent 深入（2 篇：工具调用/多智能体） ----------
    {
        "id": "agent_tool_calling",
        "title": "Agent·工具调用：Function Calling 与工具选择",
        "text": (
            "Function Calling：模型输出结构化的函数名 + 参数（JSON），由外部代码执行后把结果返回给模型。"
            "和 ReAct 的区别：ReAct 是显式的思考-行动循环（thought 可见），"
            "function calling 是模型直接输出要调用的函数（思考过程隐式），更高效。"
            "工具选择的问题：模型可能选错工具（tool hallucination）、参数填错、或调用不存在的工具。"
            "解法：工具描述写清楚（名称、作用、参数、示例）；代码层校验模型返回的工具名是否在允许集合；"
            "约束生成（constrained generation）限制模型只能选合法工具。"
            "并行工具调用：多个工具无依赖时可以并行调用，减少延迟。"
            "追问点：怎么防止模型调用不存在的工具？——代码层白名单校验，模型返回非法工具名就降级或重试。"
            "易错点：以为 function calling 只能用于「查天气」这类简单场景，它其实是 Agent 工具调用的基础设施。"
        ),
    },
    {
        "id": "agent_multi_agent",
        "title": "Agent·多智能体协作：Orchestrator 与 Worker 模式",
        "text": (
            "多智能体协作的动机：单 Agent 面对复杂任务时，上下文压力大、职责混乱，"
            "拆成多个专职 Agent 各自负责一块（检索、计算、代码、写作）更清晰。"
            "Orchestrator-Worker 模式：一个主 Agent（编排者）负责任务分解和调度，"
            "多个 Worker（执行者）各司其职，主 Agent 汇总结果。"
            "多智能体的代价：多次 LLM 调用（成本高）、Agent 间通信（上下文传递容易失真）、"
            "协调开销（谁负责什么、结果冲突怎么裁决）。"
            "什么时候需要多 Agent：任务可清晰拆分成多个独立子任务、且单 Agent 上下文不够用的时候。"
            "追问点：单 Agent + 代码状态机 和 多 Agent 怎么选？——任务简单、流程固定用单 Agent + 状态机，"
            "任务复杂、需要并行或分工用多 Agent。过度设计多 Agent 是常见误区。"
            "易错点：为了「多 Agent」而多 Agent，忽略它带来的成本和通信复杂度。"
        ),
    },

    # ---------- 后端基础（4 篇：HTTP/缓存/消息队列/网络） ----------
    {
        "id": "backend_http_status",
        "title": "后端·HTTP：状态码、幂等与 RESTful 设计",
        "text": (
            "HTTP 状态码分类：2xx 成功（200 OK、201 Created、204 No Content）；"
            "3xx 重定向（301 永久、302 临时、304 未修改）；"
            "4xx 客户端错误（400 参数错、401 未认证、403 无权限、404 不存在、429 限流）；"
            "5xx 服务端错误（500 内部错、502 网关错、503 不可用）。"
            "幂等性：同一请求执行多次和执行一次效果相同。GET/PUT/DELETE 幂等，POST 不幂等。"
            "RESTful 设计原则：资源用名词（/users 而非 /getUser）、HTTP 动词表达操作、"
            "状态码表达结果、无状态（每个请求自包含）。"
            "追问点：PUT 和 PATCH 的区别？——PUT 整体替换（幂等），PATCH 部分更新（不保证幂等）。"
            "易错点：用 POST 做所有操作；或把 401（未认证）和 403（无权限）搞混。"
        ),
    },
    {
        "id": "backend_cache_pattern",
        "title": "后端·缓存：Cache Aside、穿透击穿与一致性",
        "text": (
            "Cache Aside（旁路缓存）模式：读——先查缓存，命中返回，未命中查库、写缓存、返回；"
            "写——先更新数据库，再删缓存。这是最常用的缓存模式。"
            "为什么写是「删缓存」而不是「更新缓存」：更新缓存可能写入脏数据，删缓存让下次读重建，更简单可靠。"
            "缓存一致性问题：先更新库再删缓存，删缓存失败会导致缓存和库不一致——"
            "用延迟双删、消息队列重试删缓存、或设置缓存过期兜底。"
            "缓存预热和缓存击穿：热点数据提前加载（预热）；热点 key 过期瞬间大量请求打库（击穿），用互斥锁解决。"
            "追问点：为什么不用「先删缓存再更新库」？——这会导致更新库之前，并发读把旧数据写回缓存。"
            "易错点：把缓存穿透（查不存在的数据）和缓存击穿（热点 key 过期）混为一谈。"
        ),
    },
    {
        "id": "backend_mq",
        "title": "后端·消息队列：削峰、解耦与异步",
        "text": (
            "消息队列（MQ）解决的三个问题：削峰（高并发时把请求放入队列，后端按能力消费，避免打垮）；"
            "解耦（生产者消费者不直接调用，系统间通过队列通信）；异步（耗时操作异步化，提升响应速度）。"
            "常见 MQ：Kafka（高吞吐、日志、大数据场景，持久化强）、RabbitMQ（功能全、路由灵活、金融常用）、"
            "RocketMQ（阿里系、事务消息、电商常用）。"
            "消息不丢失的三道防线：生产端确认（producer ack）、Broker 持久化、消费端确认（consumer ack）。"
            "重复消费：MQ 至少一次投递保证，消费端要幂等（唯一 ID 去重）。"
            "追问点：消息积压了怎么办？——先扩容消费者、再排查消费慢的原因，紧急情况临时丢弃非核心消息。"
            "易错点：以为 MQ 能解决所有一致性问题（它是异步的，有延迟和重复，一致性要额外设计）。"
        ),
    },
    {
        "id": "backend_network",
        "title": "后端·网络：TCP/UDP、粘包与 TIME_WAIT",
        "text": (
            "TCP 和 UDP 的区别：TCP 面向连接、可靠（握手、重传、流量控制）、有序、面向字节流；"
            "UDP 无连接、不可靠、无序、面向数据报、快——适合实时音视频、DNS、游戏。"
            "TCP 粘包/拆包：TCP 是字节流，没有消息边界，多条消息可能粘在一起或一条被拆开——"
            "解决：固定长度、分隔符、或消息头带长度字段。"
            "TIME_WAIT 状态：主动关闭方在关闭后等待 2MSL（两倍最大报文生存时间），"
            "确保最后的 ACK 能到达、且旧连接的数据包都消散。大量 TIME_WAIT 会占端口，"
            "用 SO_REUSEADDR 或减少短连接解决。"
            "追问点：为什么握手三次、挥手四次？——握手三次能确认双方收发能力，挥手四次因为全双工要各自关闭。"
            "易错点：以为 UDP 有「连接」（它只是 sendto/recvfrom）；或把 TIME_WAIT 当 bug 要消灭。"
        ),
    },

    # ---------- 算法（3 篇：排序/树/图） ----------
    {
        "id": "algo_sort",
        "title": "算法·排序：快排、归并与堆排",
        "text": (
            "快速排序：选 pivot，把小于 pivot 的放左边、大于的放右边，递归两边。"
            "平均 O(nlogn)，最坏 O(n^2)（pivot 每次选到最大/最小）。优化：随机 pivot、三数取中。"
            "归并排序：分治，把数组分成两半各自排序再合并。稳定、O(nlogn)、需要 O(n) 额外空间。"
            "堆排序：建最大堆，反复取堆顶（最大值）放到末尾。O(nlogn)、原地、不稳定。"
            "稳定性：相等元素排序后相对顺序不变。归并稳定，快排/堆排不稳定。"
            "追问点：快排为什么最坏 O(n^2)？——pivot 选得不好，每次只把问题规模减 1，退化成 O(n) 层 × O(n) 每层。"
            "易错点：把「稳定排序」理解成「排序结果稳定不变」，它指的是相等元素的相对顺序。"
        ),
    },
    {
        "id": "algo_tree",
        "title": "算法·二叉树：遍历、平衡与 BST",
        "text": (
            "二叉树遍历：前序（根左右）、中序（左根右）、后序（左右根）、层序（BFS 队列）。"
            "递归实现简单，迭代实现用栈模拟。中序遍历二叉搜索树（BST）得到有序序列。"
            "二叉搜索树（BST）：左子树 < 根 < 右子树。查找/插入/删除平均 O(logn)，"
            "但退化成链表（顺序插入）后是 O(n)。"
            "平衡二叉树：AVL（严格平衡，左右子树高度差 ≤1，旋转维护）、"
            "红黑树（弱平衡，旋转少，插入删除快，Java TreeMap、C++ map 底层）。"
            "追问点：红黑树 vs AVL？——红黑树牺牲一些平衡换更少的旋转，适合插入删除频繁的场景；"
            "AVL 查询更快，适合查询多、更新少的场景。"
            "易错点：把「平衡」理解成左右子树节点数相等（它指的是高度差有限）。"
        ),
    },
    {
        "id": "algo_graph",
        "title": "算法·图：最短路径、拓扑排序与并查集",
        "text": (
            "图遍历：DFS（栈/递归，找路径、连通分量）、BFS（队列，无权图最短路径）。"
            "最短路径：Dijkstra（加权无负边，贪心，O(ElogV)）；Bellman-Ford（可处理负权边，O(VE)）；"
            "Floyd（多源，O(V^3)，动态规划）。"
            "拓扑排序：有向无环图（DAG）的线性排序，依赖关系问题（课程表、编译顺序）用 Kahn 算法（入度 BFS）或 DFS。"
            "并查集（Union-Find）：管理不相交集合，路径压缩 + 按秩合并后接近 O(1)，"
            "用于连通性判断、最小生成树 Kruskal 算法。"
            "最小生成树：Kruskal（边排序 + 并查集）、Prim（顶点扩展）。"
            "追问点：Dijkstra 为什么不能有负权边？——它基于「已确定最短路的点不会更短」的贪心假设，负权边会破坏这个假设。"
            "易错点：把 Dijkstra 用到有负权边的图上（应该用 Bellman-Ford）。"
        ),
    },

    # ---------- 安全（2 篇：Web 安全/认证） ----------
    {
        "id": "security_web",
        "title": "安全·Web 安全：XSS、CSRF 与 SQL 注入",
        "text": (
            "XSS（跨站脚本）：攻击者注入恶意脚本，在用户浏览器执行。"
            "存储型（脚本存到数据库，所有访问者中招）、反射型（脚本在 URL 里，诱导点击）、DOM 型（前端代码漏洞）。"
            "防御：输出转义（渲染前转义 HTML）、CSP（内容安全策略限制脚本来源）、HttpOnly Cookie。"
            "CSRF（跨站请求伪造）：诱导用户在已登录状态下点击恶意链接，用用户的身份发请求。"
            "防御：CSRF Token（表单带随机 token 验证）、SameSite Cookie、校验 Referer。"
            "SQL 注入：拼接 SQL 字符串，攻击者输入恶意 SQL 片段。防御：参数化查询（预编译语句），永远不要拼字符串。"
            "追问点：XSS 和 CSRF 的区别？——XSS 是「在用户浏览器执行恶意代码」，CSRF 是「冒充用户发请求」，本质不同。"
            "易错点：只转义了「可见内容」忘了转义属性值；或以为 HTTPS 能防 XSS（它防的是传输层窃听）。"
        ),
    },
    {
        "id": "security_auth",
        "title": "安全·认证授权：JWT、Session 与 OAuth",
        "text": (
            "Session 认证：服务端存会话状态（Redis/内存），客户端只带 session_id（Cookie），"
            "服务端每次查会话。好处：可随时吊销；坏处：服务端有状态，分布式要共享会话。"
            "JWT（无状态令牌）：token 本身含用户信息，服务端只验签不查库。"
            "好处：无状态、易扩展；坏处：签发后无法主动吊销（除非加黑名单），token 泄露影响期长。"
            "JWT 三部分：header（算法）、payload（claims，含 exp/sub）、signature（签名）。"
            "注意：payload 只是 base64 编码不是加密，别放敏感信息。"
            "OAuth2：第三方授权协议，授权码模式（最安全）、密码模式、客户端模式。"
            "追问点：JWT 怎么吊销？——短有效期 + 黑名单，或 refresh token 轮换。"
            "易错点：把 JWT 当成加密（它只是签名防篡改，不保密）；或把 access token 设太长有效期。"
        ),
    },
]

